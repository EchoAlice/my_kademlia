use tokio::sync::mpsc;

pub struct Service {
    // node_channel
    // pub socket: Arc<UdpSocket>,
    // pub outbound_requests: HashMap<Identifier, (Message, mpsc::recieve<bool>)>,
}

impl Service {
    // Bind UDP Port here!
    pub fn new() {}
}
